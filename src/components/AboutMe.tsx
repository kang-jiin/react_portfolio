import React from 'react';
import {
    Button,
    Container,
    Nav,
    Navbar,
} from 'react-bootstrap';

function AboutMe() {
    return (
        <Container>
            WEB DEVELOPER
        </Container>
    );
}

export default React.memo(AboutMe);
